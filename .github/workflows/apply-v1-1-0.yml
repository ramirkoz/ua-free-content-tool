name: Apply v1.1.0 implementation

on:
  push:
    branches:
      - feature/v1.1.0-bilingual-learning-inbox
    paths:
      - tools/apply_v1_1_0.py
      - tools/prepare_v1_1_0_migration.py
      - .github/workflows/apply-v1-1-0.yml
  pull_request:
    branches:
      - main
    paths:
      - tools/apply_v1_1_0.py
      - tools/prepare_v1_1_0_migration.py
      - .github/workflows/apply-v1-1-0.yml
      - content_agent/i18n.py
      - content_agent/ui/topic_candidates_dialog.py
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-and-test:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: windows-latest

    steps:
      - name: Check out feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/v1.1.0-bilingual-learning-inbox
          fetch-depth: 0

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        shell: pwsh
        run: |
          python -m pip install --disable-pip-version-check -r requirements.txt -r requirements-test.txt

      - name: Align migration anchors
        run: python tools/prepare_v1_1_0_migration.py

      - name: Apply deterministic source migration
        run: python tools/apply_v1_1_0.py

      - name: Compile all Python sources
        run: python -m compileall -q .

      - name: Run complete test suite
        run: python -m pytest -q

      - name: Verify entrypoint imports
        run: python tools/check_entrypoint_imports.py

      - name: Import application
        run: python -c "import content_agent.main"

      - name: Commit validated implementation
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if (git diff --cached --quiet) {
            Write-Host "No implementation changes to commit."
            exit 0
          }
          git commit -m "Implement bilingual learning inbox v1.1.0 [skip ci]"
          git push origin HEAD:feature/v1.1.0-bilingual-learning-inbox
