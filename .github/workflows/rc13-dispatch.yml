name: RC13 dispatch

on:
  push:
    branches: [main]
    paths: [.github/workflows/rc13-dispatch.yml]

permissions:
  contents: write
  actions: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Dispatch canonical RC13 sync
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          gh workflow run rc13-canonical-sync.yml --ref main
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm .github/workflows/rc13-dispatch.yml
          git commit -m 'Remove RC13 dispatch helper'
          git push origin HEAD:main
